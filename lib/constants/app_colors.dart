import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1E2328);
  static const Color surface = Color(0xFF2A2E34);
  static const Color card = Color(0xFF3B3F46); // Slightly lighter grey for differentiation if needed
  static const Color primary = Color(0xFFF5B301);
  static const Color secondary = Color(0xFFFED053);
  static const Color text = Colors.white;
  static const Color textSecondary = Colors.grey;
}
